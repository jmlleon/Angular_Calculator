import { SetButtonFocusDirective } from './set-button-focus.directive';

describe('SetButtonFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SetButtonFocusDirective();
    expect(directive).toBeTruthy();
  });
});
